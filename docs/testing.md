# Testing

```
python -m pytest tests -q          # full suite (427 tests, ~150 s)
```

Runs pygame-free (model) and with `SDL_VIDEODRIVER=dummy` (view/app/terrain).

## Layout

| File                  | Covers                                                |
|-----------------------|-------------------------------------------------------|
| `test_physics.py`     | sub-stepping, tunnelling guards, clamps, circle hits   |
| `test_items.py`       | weighted drops, RNG reproducibility, bounds, **cosmetic phase is reproducible yet never shared by a cluster — and never drawn from the game RNG**, so a renderer detail cannot shift the simulation stream |
| `test_entities.py`    | per-kind enemy movement (incl. darter dive geometry), boss patterns, bullets |
| `test_new_enemies.py` | the four specialists and the decisions they force: splitter → exactly `split_count` shards fanned left/right and **no chain-splitting**, shards weaker + diverging, bomber shells are fat/slow, burst only after `shell_fuse` and throw a radial fragment fan, rammer slides its lock toward the player then **commits to the frozen column** (a last-second dodge is not followed) and never fires, only the bomber lobs shells, all three introduced with sectors to spare, **shards never appear in a spawn table**, kind → sheet → baked-file wiring |
| `test_game.py`        | spawn director, collisions, item apply, bomb (incl. `BOMB_BOSS_DAMAGE`), mine kills vs shield, full 5-level smoke run |
| `test_app_menu.py`    | **mouse-click menu regressions** (quit-confirm flow must not be double-activated); save data redirected to `tmp_path` via `C.TEST_DATA_DIR` |
| `test_app_window.py`  | **display lifecycle regressions**: `set_mode` never runs after startup; fullscreen via `toggle_fullscreen()` (reverts setting on failure), window scale via `Window.size`, vsync applies next launch, viewport letterbox math in logical space, audio-only changes never touch the window |
| `test_terrain.py`     | baked PNGs shipped+match procedural bake for every scheduled (theme, level-seed) pair, all themes build/draw, determinism, wrap offset bounded, no HUD bleed, procedural fallback, phase schedule weights/boundaries, dissolve changes art then settles, empty phases rejected, renderer builds TerrainTrack |
| `test_audio_music.py` | music build-plan ordering (menu, this run's sector cue, one boss, then round-robin so **no sector waits behind another sector's whole pool**, and every cue exactly once), incremental per-track publishing, buffers keyed by cue name, one broken cue must not kill the playlist (render stubbed, no mixer), a typo in `LEVEL_CUES` costs one cue instead of the soundtrack, and sector choice itself: a draw never leaves the sector, never repeats the cue that just played, prefers variety over the identity cue once anything else is ready, falls back to any ready cue when the sector has none, and goes silent rather than crashing when nothing is ready |
| `test_drum_kit.py` | the drum layer as signal, not as code: every kit renders every voice inside the same peak band and is bit-identical per `(name, rate)`; brightness is ordered (kick < snare < hat) and every voice starts loud and ends quiet; `play()` puts energy exactly at its offset, scales with velocity, clamps a tail that runs past the buffer, ignores a hit beyond it and **never raises on an unknown voice**; tom pitch selection is not backwards; groove table is playable data (steps on-grid, velocities in range, fill voices exist); swing moves odd 16ths only and never the downbeat; fills replace the snare backbeat, rotate between the A and B phrases and descend in pitch; boss bars crash on bar 1 only and a boss intro is a real roll of distinct hits; every cue's `groove`/`kit`/`scale` tag names a real entry and the roster spans ≥4 grooves and ≥5 modes; the soft-clip master leaves **no sample pinned at full scale and adds no extra zero crossings** (that flip was the crackle); and the kit is audible in a real render as transients, not as a pad lift |
| `test_instruments.py` | each instrument on a property it is named for (measured with a hand-rolled DFT and zero-crossing counters, not with clips): buffers come out the requested length and stay bounded, degenerate requests do not raise, **no two instruments render the same waveform**, flutes and plucks hold the requested pitch through the middle of a note, the flute's vibrato is absent at the lip of the note and present by its end, the violin starts slower than the flute and the pluck starts faster than the violin, a pluck loses its top partial as it dies (a string, not a held organ chord), the bell's 3-2 partial balance moves as the modulator decays (inharmonic, not additive), the string section beats against itself where a single unison bow cannot, and the note cache is keyed by material rather than caller, hands each caller its own copy, and bounds its own memory. `phrase()` is measured the same way, as a player rather than a generator: a connected note travels in from the previous pitch while a detached one does not, a slide **lands** on its target and stops there, an octave leap is placed clean instead of slid, `portamento=0.0` switches travel off, a bowed note is cut when the next begins while a plucked line keeps ringing, a join does not click, and dead or out-of-order notes are handled || `test_music_arrangement.py` | the melody and bass side of musical identity: `MOTIF_SETS`/`BASS_LINES` are playable data (motif slots are degree/route/hold labels, bass slots are in range, every referenced motif exists), each family keeps the **fingerprint that defines it** (the hero set opens on a leap of at least an octave, the lyric set must not, and neither starts where the other does), the chromatic approach tone really is a semitone below the root under every chord, and — through the renderer's own `VoiceLog`, not by reading tables — a cue's tags reach the synth: a hero cue leads with hero material, a tagged bass line produces octave pops and approach tones, those pops are louder than the root they answer, an untagged cue keeps the stock bass note-for-note, and the tagged cues differ from untagged renders in more than a third of their samples | Per-sector pools are checked here too: every sector has 3-5 cues, every cue belongs to at least one pool, no pool reuses a cue, identities are distinct per sector, shared cues register with both sectors, and `pick_level_cue` is tested as a pure function (ready-only, sector-only, no-repeat). Voicing is checked where it lands: instrument names in the table are real and sit in real slots, at least ten cues stay plain synths on purpose, a voiced slot goes through the instrument path and replaces its wave notes, a wave name in a slot stays a wave, `lead_b` changes the B section and leaves the A section byte-identical, and a voiced cue differs from itself unvoiced | 
| `test_seamless_sectors.py` | the sector change without the load hitch: a track adopts prebuilt zone maps and refuses a mismatched schedule, the zone plan follows the sector's wave length, the prefetch worker builds a whole sector off-thread and is idempotent while queued, **a take transfers ownership** (the cache must never share scrolling maps with a live track), a broken sector neither kills the worker nor blocks later ones, art warm-up is counted in `pending()` like sectors are, the sweep starts at the top of the field, scrolls both sectors, freezes with the game and outlasts the SECTOR CLEAR banner, the renderer promotes the incoming sector when the edge lands and prefers a prefetched track over building one, and the model exposes the wave clock, the boss's hull fraction and a sector's boss art so the app can warm work at the half-wave and half-hull checkpoints, and **losing a ship never moves the ground** — `ensure_terrain` keeps the track of the sector already on screen (same `sector_key`, a handoff mid-sweep included), a new sector still builds, and the app-level test drives a real death through the SHIP DOWN banner to assert the very same track is still installed and scrolled *further* than before it |
| `test_state_machine.py` | **transition table as traffic law**: every state declared, SHUTDOWN reachable from everywhere and terminal, no self-transitions, full winning + losing run walkable, submenus open from title/pause only, **PLAYING→VICTORY** (regression: the final SECTOR CLEAR asked for VICTORY from PLAYING, the table refused it and the win silently never landed), illegal moves raise and leave state untouched |
| `test_scores.py` | leaderboard storage: round-trip, missing file ≠ corruption, corrupt file quarantined then ignored, junk rows dropped, ranking/trim/tie order, `rank_of` on empty + partial + full tables, initials sanitiser |
| `test_high_scores_app.py` | leaderboard through the real App: title/pause entry points, banner → `NAME_ENTRY` for a placing run, typing/arrows/backspace, Enter signs the table (row + highlight), Esc leaves it anonymous, a non-placing run skips entry |
| `test_reinforcements.py` | boss escort waves: silent before the boss, first wave after `ESCORT_FIRST_WAVE`, **launches stop the instant the boss dies** (no farming), on-screen cap per sector, small craft only, seeded positions, cadence ramps to its clamp; power-ups carry across sectors but reset on death/new game |
| `test_boss_music.py` | six boss cues are flagged `drive`, faster than every level cue, denser bed than the same bar without drive, render audible short loops; **boss pool is third in the audio build plan**, keys published per pool, `play_boss_music()` reports false until live and never repeats itself |
| `test_boss_habits.py` | the nine bosses are nine fights: every sector maps to a habit, **no two bosses share a movement strategy or an attack sequence**, the entry is an entrance and not a teleport (side bosses start outside the field), **a boss is silent until it lands**, volleys follow the habit's declared order and downward volleys actually travel down, a wall volley always leaves a 90 px corridor, cluster shells only come from the shell primitive, enrage quickens the rhythm instead of calming it, a fighting boss never leaves its band, and the same seed reproduces the same fight |
| `test_shield_fx.py` | the bubble never hides the craft: baked bubble is wider than the hull, **not one of the hull's own pixels moves when the shield is on** (measured against the renderer's own hull mask; drawing the bubble on top changes them by 30+ levels), the deploy pop happens once per shield and is derived rather than stored, the threat flare lights on the side the shot is coming from and ignores your own gunfire, and rendering the effect spends none of the run's seeded RNG. The **lapse** is covered as its own promise: the bubble is solid until `SHIELD_WARN_TIME` remains, then strobes at `SHIELD_FLASH_HZ` without ever blinking fully out (a shield that vanishes mid-flash reads as gone one flash early), then fades smoothly over `SHIELD_FADE_TIME`; the strobe is periodic rather than random (a counted number of flips in a fixed span), the fade is monotonic, and the whole rule is presentation — it consumes no RNG and does not shorten the countdown by a frame. High-contrast pen mode falls back to the same lapse rule on its ring |
| `test_moon_weapon.py` | the third power track and the bullet that does not stop: a moon pod raises `moon_level` **and nothing else**, caps at `MOON_MAX_LEVEL`, is beneficial and really drops, and dies with the craft; the volley grows with the track, is centred on the hull, fires on its own clock (measured as volleys per two seconds, not a machine gun), and level 1 already throws two blades because one weak blade is not a weapon; the weapon may not be a bigger gun in disguise (`MOON_DMG ≤` a shell, `MOON_SPEED <` every shell, `MOON_INTERVAL >` the Vulcan's); pierce is tested as a walk through a column — **each hull charged exactly once** while the blade carries on and survives, a shell by contrast is spent on the first hull and never reaches the one behind, a boss hit six frames running takes one hit, and a blade that kills in front still kills behind it; the memory is asserted to hold the **object** and a stranger at the same coordinates is not in it, while ordinary shots allocate nothing; art coverage (`shot_moon` sheet + frames, the pod appended tiles 36-39 instead of shuffling the nine that shipped, one glyph per kind), the renderer's sheet choice, the pen-mode fallback (a crescent is hollow at the middle and painted at the belly — a disc would lie about the shape), and the HUD readout that stays silent at level 0 and never covers the missile readout next to it |
| `test_sprites.py`   | sprite-sheet manifest integrity (tile addressing round-trips, every anim tile in-bounds), baked sheets shipped + **match fresh procedural bake** (stale-art detector), painter determinism, white hit-flash silhouette keeps alpha mask, `tinted()` recolours and keeps the alpha mask (+ per-tint cache), explosion playhead lifecycle/cap in `Effects`, **whip model**: attaches, replaces Vulcan, expires, forward reach, arc within `WHIP_LEN`, **pickup art**: every kind owns 4 animating tiles, every pod covers its collision circle (art is never smaller than the hitbox), the shared aura orbits and is hollow at the centre, pod accent / aura tint / vector colour all agree with `ITEM_COLORS`, renderer integration (sprites+fallback, plasma switch at weapon lvl 4, pod art → glyph fallback with no sheets, high-contrast draws vectors without a bank) |

## House rules

- Model tests instantiate `Game`/entities with explicit `random.Random(seed)`
  — any test flakiness means a determinism leak (global `random` usage).
- Any test touching the real App must monkeypatch `C.TEST_DATA_DIR` first
  (never write user save files from tests).
- pygame-display tests post synthetic `pygame.event.Event`s and call
  `app.process_events()` directly — no real window needed.
- `conftest.py` installs an autouse fixture that guarantees a pygame display
  exists (32×32 scratch surface under the dummy driver) before any test blits.
  Without it, `test_terrain.py` passed in the full suite and failed when run
  alone — suites that only pass together hide an import-order dependency.
- Table files are tested as data: `test_sprites.py` walks `sheet_table.SPRITES`
  (tile addressing round-trips, every anim tile in bounds), `test_terrain.py`
  parametrizes over every `StageTheme`, `test_state_machine.py` over every
  `TRANSITIONS` entry. Adding a row without its art/rule is a red test, not a
  runtime surprise.

## Headless smoke of the real app

```
SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy \
    python main.py --self-test 240    # frames, exits 0
```

## Quality gates (all clean as of the dead-code / table-file cleanup)

- `ruff check src tests scripts main.py` → 0 errors
- `mypy --ignore-missing-imports src scripts main.py` → 0 errors (98 source
  files; the extra modules are the persistence package and the table files)
- pytest → 427 passed (boss habits, shield bubble, terrain phases, sprites/whip/pickup pods, specialists, drum kit and grooves, melodic identity, seamless sector handoff and prefetch included)
- `python scripts/audit_symbols.py` → 0 flags (declarations in `src/` that nothing references; the census counts table keys and strings as uses, exit code 1 when anything is flagged)
- self-test → 240 frames, exit 0

The gate is the four commands above, in that order; `pytest`'s exit code is the
verdict (its summary line does not always survive a pipe on Windows).
